## 🔴 ПАМЯТЬ (Memory MCP) — КРИТИЧЕСКИ ВАЖНО

### Обязательное поведение

Память должна использоваться агентом **автоматически и проактивно**:

- Определять, когда контекст из памяти релевантен текущей задаче
- Доставать из памяти только нужные элементы (минимально достаточный набор)
- Решать, что сохранять (только долгоживущие, повторно полезные факты/правила)
- Обновлять память, если пользователь меняет правило/предпочтение
- Использовать память как часть стандартного процесса, а не "по запросу"

Дополнительно (обязательно):

- Всегда применять приоритет источников: `текущий запрос пользователя > locked preferences > свежие verified lessons > старые lessons`
- При конфликте памяти с текущим запросом пользователя — следовать текущему запросу и обновлять память
- Не засорять память: не сохранять одноразовые шаги, временные пути, сырые логи, секреты, токены, пароли
- До записи проверять дубликаты по `key` и смыслу (использовать upsert, а не создавать новые похожие записи)
- При ошибке памяти не падать: выполнять задачу, копить pending-уроки и дозаписывать после восстановления

### Обязательные проверки

| Когда | Действие |
|-------|----------|
| Старт сессии | `memory_memory_health()` (если ошибка: 2 ретрая с backoff, далее degraded mode) |
| Приветствие | `memory_memory_search_preferences(query="greeting")` |
| Перед задачей | `memory_memory_search_lessons(query="ключевые слова")` + при необходимости `memory_memory_search_preferences(query="тема")` |
| При смене правила пользователем | Немедленно `memory_memory_upsert(type="preference")` с тем же key (upsert) |
| После задачи | `memory_memory_upsert(type="lesson")` только для подтвержденных долгоживущих выводов |
| Конец сессии | `memory_memory_consolidate(session_id=..., dry_run=false)` |
| Периодически (обслуживание) | `memory_memory_ttl_cleanup(dry_run=false)` |

### ⚠️ ГЛАВНОЕ ПРАВИЛО

**ВСЁ релевантное из памяти — ОБЯЗАТЕЛЬНО применять.**

Если есть конфликт:

1. Текущая явная инструкция пользователя
2. Locked preference
3. Свежий подтвержденный lesson
4. Старый lesson

При конфликте обязательно обновить устаревшую запись памяти.

### Инструменты памяти

| Инструмент | Назначение |
|------------|------------|
| `memory_memory_search` | Поиск по workspace файлам (FTS + векторы) |
| `memory_memory_search_lessons` | Поиск уроков по ключевым словам |
| `memory_memory_search_preferences` | Поиск предпочтений по ключевым словам |
| `memory_memory_search_all` | Поиск по lessons И preferences |
| `memory_memory_list` | Список всех lessons или preferences |
| `memory_memory_upsert` | Сохранение урока или предпочтения |
| `memory_memory_get` | Получение конкретной записи по ключу |
| `memory_memory_delete` | Удаление записи |
| `memory_memory_consolidate` | Консолидация эпизодов в уроки |
| `memory_memory_index_workspace` | Индексация файлов workspace |
| `memory_memory_health` | Статус системы |
| `memory_memory_ttl_cleanup` | Очистка по TTL |

### Типы данных

| Тип | Описание | TTL |
|-----|----------|-----|
| `lesson` | Уроки, выводы, паттерны | 90 дней |
| `preference` | Предпочтения пользователя | 180 дней |
| `episode` | Эпизоды сессии | 60 дней |

### Что сохранять / что не сохранять

Сохранять:

- Устойчивые пользовательские предпочтения (стиль, формат, ограничения)
- Повторно полезные технические уроки (фиксы, runbook, root-cause)
- Проверенные проектные договоренности (контракты, совместимость)

Не сохранять:

- Временные артефакты и одноразовые действия
- Неподтвержденные гипотезы
- Секреты, токены, ключи, персональные данные без явной необходимости

### Стандарты качества записи

- `key`: стабильный, snake_case, без дат в начале, с предметной областью
- `value`: 1-3 предложения, конкретно "когда/что/почему"
- `meta`: минимум `{"context": "...", "confidence": "high|medium|low", "source": "user|system|test"}`
- На одну завершенную задачу: обычно 0-2 lessons (не больше 3 без необходимости)

### Пример сохранения

```python
memory_memory_upsert(
    key="unique_key",
    value="Текст урока/предпочтения",
    type="lesson",  # или "preference"
    meta={"context": "..."}
)
```
